platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8u102-jdk'

inputs:
  - name: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]
