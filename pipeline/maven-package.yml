platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker.io/java
    tag: latest

inputs:
  - name: git-repo

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "-DskipTests=true", "package"]
