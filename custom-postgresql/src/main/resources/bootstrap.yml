---
spring:  
  application:
    name: postgresql
info:
  app:
    name: PostgreSQL Service Broker
    description: PostgreSQL Service Broker for Cloud Foundry Service Deployments
    version: 0.0.1
    
---
spring:
  profiles: default,development
  cloud:
    config:
      uri: http://localhost:8081
---      
spring:
  profiles: cloud
  cloud:
    config:
      uri: https://config.onevoila.rocks
      
            